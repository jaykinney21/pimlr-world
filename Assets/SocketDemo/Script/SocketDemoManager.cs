using UnityEngine;

public class SocketDemoManager : MonoBehaviour
{
    //public static SocketDemoManager Instance;

    //public SocketPlayerManager PlayerManager;
    //public SocketUIManager UIManager;

    /*private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(Instance);
        }
        else
        {
            Debug.Log("DestroyImmediate");
            //DestroyImmediate(Instance);
        }
    }*/
}
